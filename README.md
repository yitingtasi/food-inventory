# 🥗 食材庫存系統

一個具備 LINE Notify 智慧通知的食材庫存管理系統。

## 📁 專案結構

```
food-inventory-server/
├── server.js          ← Express 後端（LINE Notify 代理）
├── public/
│   └── index.html     ← 前端應用（所有使用者開此連結）
├── .env.example       ← 環境變數範本
├── package.json
└── README.md
```

## 🚀 快速啟動

### 方法一：本機執行（測試用）

```bash
# 1. 安裝依賴
npm install

# 2. 啟動伺服器
npm start

# 3. 瀏覽器開啟
http://localhost:3000
```

### 方法二：部署到雲端（推薦，讓所有人都能使用）

#### ▶ 部署到 Railway（免費，最簡單）

1. 前往 https://railway.app 並登入
2. 點「New Project」→「Deploy from GitHub」
3. 把此資料夾上傳到你的 GitHub repo
4. Railway 自動部署，取得公開 URL（例如 `https://your-app.railway.app`）
5. 把此 URL 分享給所有使用者即可

#### ▶ 部署到 Render（免費）

1. 前往 https://render.com
2. New → Web Service → 連結 GitHub repo
3. Build Command: `npm install`
4. Start Command: `npm start`
5. 部署完成後取得 URL

#### ▶ 部署到 VPS（完整控制）

```bash
# 伺服器上執行
git clone <your-repo>
cd food-inventory-server
npm install
npm install -g pm2      # 背景執行
pm2 start server.js --name food-inventory
pm2 save
```

若需要 HTTPS（必要），使用 Nginx 反向代理 + Let's Encrypt SSL。

---

## 📲 LINE Notify 設定

1. 前往 https://notify-bot.line.me/zh_TW/
2. 登入後點「發行權杖」
3. 輸入服務名稱（例：食材庫存系統）
4. 選擇要接收通知的 LINE 群組或個人聊天
5. 複製產生的 Token
6. 在系統中點「⚙️ 設定」→ 貼上 Token → 儲存

---

## 🔔 通知規則

| 條件 | 通知內容 |
|------|----------|
| 庫存 ≤ 警告閾值 | ⚠️ 低庫存警告 |
| 庫存歸零 | 🚨 緊急補貨通知 |
| 預估 ≤ 3 天用完 | 📊 智慧採購預警 |

---

## 🛠️ API 端點

| 方法 | 路徑 | 說明 |
|------|------|------|
| POST | `/api/line-notify` | 發送 LINE 通知 |
| GET | `/api/health` | 伺服器健康檢查 |

### POST /api/line-notify

```json
{
  "token": "你的LINE_Notify_Token",
  "message": "通知內容"
}
```
